name: Keep Supabase Active

on:
  schedule:
    # 每天早上 10:00 执行（北京时间 UTC+8）
    - cron: '0 2 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Visit app to keep Supabase active
        run: |
          curl -s -o /dev/null -w "%{http_code}" \
            "https://bbjzibxmxcmtrjbscilq.supabase.co/rest/v1/" \
            -H "apikey: sb_publishable_ELMfIa0BNk7WxFOKQl1bXQ_ERjl3jSX"
          curl -s -o /dev/null -w "%{http_code}" \
            "https://family-tau-seven.vercel.app"
